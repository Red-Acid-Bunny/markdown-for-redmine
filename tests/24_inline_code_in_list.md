Обычный текст, `inline code` внутри элемента списка.
Затем продолжение на новой строке.

- host - утилита для DNS-запросов.
  Входит в пакет `dnsutils` (Debian/Ubuntu).
- net - утилита для запросов в AD.
  Входит в пакет `samba-common-bin`.
