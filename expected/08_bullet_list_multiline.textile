Необходимое ПО:

* host (или nslookup) - для DNS запросов. Нужен только для настройки и отладки
* net - для запросов в AD. Входит в пакет samba-common-bin (Debian/Ubuntu). Нужен только для настройки и отладки

